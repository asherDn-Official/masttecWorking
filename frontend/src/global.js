import { createContext } from "react";

// const API_URL = "https://attendance.masttecmoulds.com/api";  
const API_URL = "http://localhost:4000";  

export default API_URL;

export  const ContentProvieder = createContext({});
